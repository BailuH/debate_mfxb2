# 数字法庭 (Digital Court)

基于 LangGraph 多智能体编排的 AI 驱动刑事庭审模拟平台。

## 项目概述

数字法庭是一个模拟中国刑事审判流程的交互式平台。系统使用多个 AI 智能体分别扮演法官、公诉人、被告/辩护律师和书记员等角色，通过 LangGraph 进行流程编排，实现逼真的庭审体验。

## 技术栈

- **Python 3.13** - 核心开发语言
- **LangGraph** - 多智能体编排与状态管理
- **LangChain** - LLM 集成框架
- **DeepSeek-V3 / DeepSeek-R1** - AI 模型（通过 OpenAI 兼容 API）
- **FastAPI** - 后端 API 服务
- **Vue.js 3** - 前端界面
- **WebSocket** - 实时通信
- **uv** - Python 包管理器

## 快速开始

### 1. 环境准备

```bash
# 克隆项目
git clone <repository-url>
cd debate_mfxb2

# 安装依赖
uv sync

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入你的 DeepSeek API 配置
```

### 2. 启动后端服务

```bash
# 方式一：使用 uvicorn
uv run uvicorn src.api.main:app --host 0.0.0.0 --port 8000

# 方式二：使用 langgraph dev（支持 LangSmith 集成）
langgraph dev
```

后端服务启动后，API 文档可访问：http://localhost:8000/docs

### 3. 打开前端 Demo

直接在浏览器中打开 `demo.html` 文件，或使用本地服务器：

```bash
python -m http.server 8080
# 访问 http://localhost:8080/demo.html
```

## Demo 前端

### 功能特性

| 功能 | 描述 |
|------|------|
| **实时庭审** | 通过 WebSocket 与后端实时通信，展示庭审进程 |
| **角色识别** | 自动识别消息来源（法官、公诉人、被告、书记员），使用不同颜色标记 |
| **进度追踪** | 显示当前庭审阶段、进度百分比、剩余轮次 |
| **争议焦点** | 动态展示审判长总结的争议焦点列表 |
| **中断响应** | 根据不同输入类型（布尔、文本、证据）动态显示交互界面 |
| **自动重连** | 连接断开后自动尝试重连（最多5次） |

### 界面布局

```
┌─────────────────────────────────────────────────────┐
│              数字法庭 - Digital Court                │
├─────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌───────────────────────────────┐ │
│  │   控制面板   │  │         庭审消息流             │ │
│  │  - 开始庭审  │  │  (实时滚动显示 AI 对话)        │ │
│  │  - 连接状态  │  │                               │ │
│  │  - 进度条    │  │  📜 消息历史...               │ │
│  │  - 当前阶段  │  │                               │ │
│  └─────────────┘  └───────────────────────────────┘ │
│  ┌─────────────────────────────────────────────┐   │
│  │         用户中断响应区                       │   │
│  │  (根据 input_type 动态显示)                  │   │
│  │  - boolean: 是/否 按钮                       │   │
│  │  - string: 文本输入框                        │   │
│  │  - evidence: 证据选择器                      │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 测试步骤

1. **启动后端**
   ```bash
   uv run uvicorn src.api.main:app --host 0.0.0.0 --port 8000
   ```

2. **打开 demo.html**
   - 双击文件在浏览器中打开
   - 或使用: `python -m http.server 8080`

3. **测试流程**
   - 等待 WebSocket 连接成功（状态显示"已连接"）
   - 点击"开始庭审"按钮
   - 观察消息流实时更新
   - 当出现"需要您的响应"提示时，根据提示类型输入
   - 查看进度条和争议焦点的变化

### 预设测试用例

Demo 内置了"程振贤过失致人死亡案"的完整测试数据：

- **案件编号**: 江公北诉字[2016]03449号
- **审理法院**: 江山市北湖区人民法院
- **被告人**: 程振贤
- **罪名**: 过失致人死亡罪
- **证据数量**: 16 份

## 项目架构

### 目录结构

```
debate_mfxb2/
├── src/
│   ├── agents/          # AI 智能体定义
│   │   ├── clerk.py     # 书记员
│   │   ├── judge.py     # 法官
│   │   ├── prosecutor.py # 公诉人
│   │   └── defendant.py  # 被告/辩护律师
│   ├── api/             # FastAPI 后端
│   │   ├── main.py      # API 入口
│   │   ├── schemas.py   # WebSocket 消息定义
│   │   ├── websocket.py # 连接管理
│   │   └── session_manager.py # 会话管理
│   ├── state.py         # 庭审状态定义
│   ├── prompt.py        # 智能体提示词
│   ├── llmconfig.py     # LLM 配置
│   └── test.py          # LangGraph 流程定义
├── demo.html            # Vue.js 前端 Demo
├── langgraph.json       # LangGraph 配置
├── CLAUDE.md            # Claude Code 开发指南
└── README.md            # 本文件
```

### 核心组件

#### 1. 状态管理 (`src/state.py`)

```python
class CourtState(BaseModel):
    messages: List[HumanMessage | AIMessage]  # 对话历史
    focus: List[str]                          # 争议焦点
    phase: PhaseEnum                          # 当前阶段
    evidence_list: List[Evidence]             # 所有证据
    current_evidence: List[Evidence] | None   # 当前举证
    meta: case_info                           # 案件元数据
    pros_question_rounds: int                 # 提问轮次
    pros_evidence_rounds: int                 # 举证轮次
    pros_focus_rounds: int                    # 辩论轮次
    focus_index: int                          # 焦点索引
```

#### 2. 多智能体系统

| 智能体 | 职责 |
|--------|------|
| **Clerk (书记员)** | 宣布法庭规则、记录程序 |
| **Judge (法官)** | 控制庭审流程、识别争议焦点、宣读判决 |
| **Prosecutor (公诉人)** | 宣读起诉书、提问、举证、进行辩论 |
| **Defendant (被告)** | 回应指控、举证、质证、辩护 |

#### 3. 庭审流程

```
START
  ↓
[开庭阶段] opening
  ├─ clerk_rules      书记员宣布规则
  ├─ judge_open       法官宣布开庭
  ├─ judge_check      核实被告身份
  └─ right_notify     权利告知
  ↓
[法庭调查] investigation
  ├─ pros_indictment       公诉人宣读起诉书
  ├─ defense_objection     被告是否异议（可中断）
  ├─ pros_question         公诉人提问 (3轮)
  ├─ defense_reply         被告回答
  ├─ defense_question      被告提问（可中断）
  ├─ pros_summary          公诉人小结
  └─ defense_summary       被告小结
  ↓
[举证阶段] investigation (续)
  ├─ judge_start_evidence  法官宣布举证开始
  ├─ pros_evidence_decision 公诉人举证决策（可中断）
  ├─ pros_show_evidence    公诉人展示证据 (3轮)
  ├─ defense_cross         被告质证
  ├─ judge_confirm         法官确认
  ├─ defense_evidence_control 被告举证决策（可中断）
  ├─ defense_show_evidence 被告展示证据
  └─ pros_cross            公诉人质证
  ↓
[法庭辩论] debate
  ├─ judge_start_debate    法官宣布辩论开始
  ├─ pros_statement        公诉人发言
  ├─ defense_self_statement 被告自行辩护
  ├─ defense_statement     辩护律师辩护
  ├─ judge_summary         法官总结
  ├─ focus                 识别争议焦点（可中断）
  ├─ pros_focus            公诉人对焦点辩论 (2轮)
  ├─ defense_focus         被告对焦点辩论
  ├─ pros_sumup            公诉人最后陈述
  └─ defense_sumup         被告最后陈述
  ↓
[判决阶段] verdict
  ├─ defense_final_statement 被告最后陈述
  └─ judge_verdict         法官宣读判决
  ↓
END
```

### API 端点

#### HTTP 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/config` | GET | 获取配置信息 |

#### WebSocket 端点

| 端点 | 描述 |
|------|------|
| `/ws/trial` | 庭审实时通信 |

#### WebSocket 消息类型

**客户端 → 服务器**

```json
// 开始庭审
{ "type": "start_trial", "data": { "case_info": {...}, "evidence_list": [...] } }

// 用户输入
{ "type": "user_input", "thread_id": "...", "data": { "interrupt_node": "...", "input": ... } }

// 心跳
{ "type": "ping" }
```

**服务器 → 客户端**

```json
// 会话创建
{ "type": "session_created", "thread_id": "...", "data": { "message": "...", "thread_id": "..." } }

// 节点执行
{ "type": "node_executed", "thread_id": "...", "data": { "node_name": "...", "state_delta": {...}, "current_phase": "...", "progress": 0.5, "messages": [...] } }

// 中断请求
{ "type": "interrupt_request", "thread_id": "...", "data": { "node_name": "...", "prompt": "...", "input_type": "boolean|string|evidence", "options": [...] } }

// 庭审完成
{ "type": "trial_completed", "thread_id": "...", "data": { "final_state": {...} } }

// 错误
{ "type": "error", "thread_id": "...", "data": { "code": "...", "message": "..." } }
```

### 中断节点（用户交互点）

系统在以下节点支持用户中断输入：

| 节点 | 输入类型 | 描述 |
|------|----------|------|
| `defense_objection_control` | boolean | 被告是否对起诉书有异议 |
| `defense_question_control` | boolean | 被告是否需要向公诉人提问 |
| `pros_evidence_decision` | evidence | 公诉人举证决策（选择证据或放弃） |
| `defense_evidence_control` | boolean | 被告是否需要举证 |
| `focus` | string | 争议焦点确认或补充 |
| `pros_focus` | string | 公诉人辩论内容 |

## 配置说明

### 环境变量

创建 `.env` 文件：

```bash
# DeepSeek API 配置
OPENAI_API_KEY=your_api_key_here
OPENAI_API_BASE=https://api.deepseek.com/v1

# 可选：LangSmith 配置（用于调试）
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_langsmith_key
LANGCHAIN_PROJECT=digital-court
```

### 常量配置

在 `src/state.py` 中定义：

```python
Q_ROUNDS = 3  # 公诉人提问轮次
E_ROUNDS = 3  # 举证轮次
F_ROUNDS = 2  # 辩论轮次
```

## 开发指南

### 运行测试

```bash
# 运行 Python 测试
python src/test.py

# 使用 LangSmith Studio 调试
langgraph dev
```

### 添加新的智能体

1. 在 `src/agents/` 创建新文件
2. 定义智能体函数，接受 `CourtState` 返回更新
3. 在 `src/test.py` 中添加节点和边

### 修改庭审流程

编辑 `src/test.py` 中的图定义：

```python
# 添加新节点
graph.add_node("new_node", new_node_function)

# 添加边
graph.add_edge("prev_node", "new_node")

# 添加条件边
graph.add_conditional_edges("new_node", routing_function, {...})
```

## 常见问题

### Q: WebSocket 连接失败
A: 确保后端服务正在运行，端口 8000 未被占用。检查浏览器控制台的错误信息。

### Q: 消息流没有更新
A: 检查后端日志，确认 LLM API 配置正确。可能是 API 额度不足或网络问题。

### Q: 中断响应无反应
A: 确保 `thread_id` 正确传递。查看浏览器开发者工具的 Network 标签确认消息格式正确。

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
